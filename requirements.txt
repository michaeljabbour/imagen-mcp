# imagen-mcp runtime dependencies.
#
# pyproject.toml is the single source of truth for dependencies. This file
# mirrors the runtime deps for `pip install -r requirements.txt` users.
# For development, install the dev extra instead:
#
#     pip install -e ".[dev]"      (or)      uv sync --extra dev
#
# The server uses the FastMCP bundled in the `mcp` SDK, not the standalone
# `fastmcp` package (see MODERNIZATION_PLAN.md Phase 8).

mcp>=1.26.0
pydantic>=2.12.3
httpx>=0.28.0
google-genai>=2.8.0
pillow>=11.0.0
